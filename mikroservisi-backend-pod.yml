apiVersion: v1
kind: Pod
metadata:
  name: backend
  labels:
    name: backend
    app: mikroservisi
spec:
  containers:
    - name: backend-1
      image: cordarjovana/mikroservisi:backend
      ports:
        - containerPort: 1337
  imagePullSecrets:
    - name: myregistrykey
