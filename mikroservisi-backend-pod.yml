apiVersion: v1
kind: Pod
metadata:
  name: backend
  labels:
    name: backend
    app: mikroservisi
spec:
  containers:
    - name: backend
      image: backend
      ports:
        - containerPort: 1337
  imagePullSecrets:
    - name: myregistrykey
