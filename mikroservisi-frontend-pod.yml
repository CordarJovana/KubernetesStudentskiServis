apiVersion: v1
kind: Pod
metadata:
  name: frontend
  labels:
    app: mikroservisi
spec:
  containers:
    - name: frontend
      image: frontend
      ports:
        - containerPort: 3000
  imagePullSecrets:
    - name: myregistrykey
